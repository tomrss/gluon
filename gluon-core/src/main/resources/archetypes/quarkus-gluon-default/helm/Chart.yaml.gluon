apiVersion: v2
name: ${project.artifactId}
description: <#if project.description??>${project.description}<#else>${project.friendlyName}</#if>
type: application
version: 0.1.0
appVersion: "${project.version}"
